<shortcuts xmlns:android="http://schemas.android.com/apk/res/android">
    <shortcut
        android:enabled="true"
        android:icon="@drawable/ic_shortcut_add"
        android:shortcutId="new_tab"
        android:shortcutLongLabel="@string/new_tab"
        android:shortcutShortLabel="@string/new_tab">
        <intent
            android:action="android.intent.action.VIEW"
            android:targetClass="arun.com.chromer.activities.NewTabDialogActivity"
            android:targetPackage="arun.com.chromer" />
        <categories android:name="android.shortcut.conversation" />
    </shortcut>
    <!-- Specify more shortcuts here. -->
</shortcuts>